#!/bin/bash

export LOG_DIR=/var/vcap/sys/log/storm
export RUN_DIR=/var/vcap/sys/run/storm
export DATA_DIR=/var/vcap/data/storm
export TMP_DIR=/var/vcap/data/storm/tmp

for dir in $LOG_DIR $RUN_DIR $DATA_DIR $TMP_DIR; do
  mkdir -p ${dir}
  chown vcap:vcap ${dir}
  chmod 700 ${dir}
done

